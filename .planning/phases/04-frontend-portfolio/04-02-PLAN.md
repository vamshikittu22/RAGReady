---
phase: 04-frontend-portfolio
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
  - src/frontend/src/components/dashboard/eval-dashboard.tsx
  - src/frontend/src/App.tsx
  - docs/demo-script.md
autonomous: false

must_haves:
  truths:
    - "README includes a Mermaid architecture diagram showing all 5 layers and data flow"
    - "README includes CI badge, quick start instructions (5 commands or fewer), and tech stack table"
    - "README links to evaluation dashboard showing metric results"
    - "Evaluation dashboard route in the app shows metric cards with pass/fail indicators"
    - "Demo video script exists documenting the 2-minute walkthrough flow"
  artifacts:
    - path: "README.md"
      provides: "Portfolio-ready README with architecture diagram, badges, quick start"
      contains: "```mermaid"
    - path: "src/frontend/src/components/dashboard/eval-dashboard.tsx"
      provides: "Evaluation metrics dashboard component"
      min_lines: 40
    - path: "docs/demo-script.md"
      provides: "Demo video script with timestamps and talking points"
      min_lines: 15
  key_links:
    - from: "README.md"
      to: "GitHub Actions CI"
      via: "badge URL"
      pattern: "actions/workflows.*badge"
    - from: "README.md"
      to: "Architecture layers"
      via: "Mermaid diagram"
      pattern: "graph TB"
    - from: "src/frontend/src/App.tsx"
      to: "src/frontend/src/components/dashboard/eval-dashboard.tsx"
      via: "Tab route rendering dashboard component"
      pattern: "EvalDashboard"
---

<objective>
Create all portfolio artifacts that make RAGReady impressive to hiring managers: a comprehensive README with architecture diagram and quick start, an evaluation dashboard showing metric results, and a demo video script.

Purpose: This transforms RAGReady from "working code" into a "portfolio piece" — the README alone should convince a hiring manager of senior-level engineering quality. Fulfills PORT-01, PORT-02, PORT-03, PORT-04, PORT-05.
Output: Professional README.md, evaluation dashboard React component, and demo video preparation materials.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-portfolio/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio-ready README with architecture diagram, badges, and quick start</name>
  <files>
    README.md
  </files>
  <action>
    Rewrite README.md as a portfolio showcase. This is the single most important file for hiring managers.

    Structure (in this order):

    1. **Hero Section:**
       - Project name: "RAGReady"
       - One-line description: "A production-grade Retrieval-Augmented Generation system with citation-enforced answers, hybrid search, automated evaluation, and CI/CD quality gates."
       - Badges row: CI status badge (`![CI](https://github.com/{user}/{repo}/actions/workflows/ci.yml/badge.svg)`), Python version badge, License badge, "Built with" badges for key tech (FastAPI, React, ChromaDB, LangChain)
       - Use placeholder `{user}/{repo}` for the GitHub URL — user will replace

    2. **Key Features** (bullet list with brief descriptions):
       - Citation-enforced generation (refuses to hallucinate)
       - Hybrid retrieval (dense + BM25 + RRF fusion)
       - Automated evaluation pipeline (7 metrics, CI quality gates)
       - Interactive React frontend (chat, citations, document management)
       - Full observability (Arize Phoenix + OpenTelemetry)

    3. **Architecture Diagram** (Mermaid — renders natively in GitHub):
       ```mermaid
       graph TB
           subgraph "Frontend"
               UI["React SPA<br/>Chat • Citations • Documents"]
           end
           subgraph "API Layer"
               API["FastAPI<br/>REST Endpoints"]
               MW["Middleware<br/>Latency Logging"]
           end
           subgraph "RAG Pipeline"
               RC["RAG Chain<br/>Grounding + Refusal Gate"]
               LLM["LLM Factory<br/>Gemini Flash + Qwen Fallback"]
           end
           subgraph "Retrieval"
               HR["Hybrid Retriever"]
               DR["Dense Search<br/>all-MiniLM-L6-v2"]
               SR["Sparse Search<br/>BM25"]
               RRF["RRF Fusion<br/>k=60"]
           end
           subgraph "Storage"
               CHROMA["ChromaDB<br/>Vector Store"]
               BM25S["BM25 Index<br/>Pickle Persistence"]
               DS["Document Store<br/>JSON Manifest"]
           end
           subgraph "Evaluation"
               EVAL["7 Metrics<br/>Faithfulness • Relevancy<br/>Recall • Precision<br/>Refusal • Citation • Hallucination"]
               GD["Golden Dataset<br/>51 Q&A Pairs"]
               CI["GitHub Actions<br/>Quality Gates"]
           end
           subgraph "Observability"
               PHX["Arize Phoenix<br/>OpenTelemetry"]
           end

           UI -->|REST| API
           API --> MW
           API --> RC
           RC --> LLM
           RC --> HR
           HR --> DR
           HR --> SR
           DR --> CHROMA
           SR --> BM25S
           HR --> RRF
           RC -.->|traces| PHX
           EVAL --> GD
           CI --> EVAL
       ```
       Show all 5 architecture layers from PROJECT.md. Make it detailed enough to be impressive but readable.

    4. **Quick Start** (5 commands or fewer):
       ```bash
       # Clone and setup
       git clone https://github.com/{user}/{repo}.git && cd ragready

       # Install Python dependencies
       pip install -e ".[eval]"

       # Start the backend
       uvicorn ragready.api.app:create_app --factory --port 8000

       # Start the frontend (in another terminal)
       cd src/frontend && pnpm install && pnpm dev
       ```
       Note: requires Ollama running locally with appropriate model, or GOOGLE_API_KEY set for Gemini Flash.

    5. **Tech Stack Table:**
       | Layer | Technology | Purpose |
       |-------|-----------|---------|
       | Frontend | React 19, TypeScript, Tailwind, shadcn/ui | Interactive chat and document management |
       | API | FastAPI, Pydantic | REST endpoints with auto-docs |
       | LLM | Gemini Flash + Qwen (fallback) | Citation-enforced generation |
       | Embeddings | all-MiniLM-L6-v2 | Dense vector representations |
       | Vector DB | ChromaDB | Persistent vector storage |
       | Retrieval | BM25 + Dense + RRF | Hybrid search with fusion |
       | Evaluation | Custom metrics (7) | Automated quality measurement |
       | CI/CD | GitHub Actions | Quality gates block regressions |
       | Observability | Arize Phoenix, OTEL | Distributed tracing |

    6. **Evaluation Results** — summary table:
       | Metric | Target | Description |
       |--------|--------|-------------|
       | Context Recall | > 0.75 | Retrieved chunks contain expected info |
       | Context Precision | > 0.70 | Retrieved chunks are relevant |
       | Faithfulness | > 0.85 | Answers grounded in evidence |
       | Answer Relevancy | > 0.80 | Answers address the question |
       | Refusal Accuracy | > 90% | Correctly refuses unanswerable questions |
       | Citation Accuracy | > 95% | Citations trace back to real chunks |
       | Hallucination Rate | < 5% | Answers don't fabricate information |

       Add note: "Run `python scripts/evaluate.py` to generate a full evaluation report."

    7. **Project Structure** — abbreviated tree showing the key directories and files:
       ```
       src/
       ├── ragready/              # Python backend
       │   ├── api/               # FastAPI routes & middleware
       │   ├── generation/        # LLM factory, RAG chain, prompts
       │   ├── ingestion/         # Document processing pipeline
       │   ├── retrieval/         # Hybrid search (dense + BM25 + RRF)
       │   ├── observability/     # Phoenix/OTEL tracing
       │   └── core/              # Models, settings, exceptions
       ├── frontend/              # React SPA
       │   └── src/
       │       ├── components/    # Chat, documents, dashboard
       │       ├── hooks/         # TanStack Query hooks
       │       └── lib/           # API client, utilities
       tests/
       ├── unit/                  # Unit tests
       ├── integration/           # Integration tests
       └── evaluation/            # Quality gate tests & metrics
       scripts/                   # CLI tools (evaluate, benchmark)
       .github/workflows/         # CI/CD pipeline
       ```

    8. **API Documentation** — brief endpoint table:
       | Method | Endpoint | Description |
       |--------|----------|-------------|
       | POST | /query | Ask a question, get cited answer |
       | POST | /documents/upload | Upload a document |
       | GET | /documents/ | List all documents |
       | DELETE | /documents/{id} | Delete a document |
       | GET | /health | System health check |

       Add note: "Full Swagger documentation available at http://localhost:8000/docs"

    9. **License** section (MIT or as appropriate — check if LICENSE file exists, if not, note as TBD)

    Keep the tone professional but approachable. Use clear headings, clean formatting. This is a portfolio piece aimed at senior engineering hiring managers.
  </action>
  <verify>
    - README.md exists and is >150 lines
    - Contains "```mermaid" block with architecture diagram
    - Contains CI badge placeholder
    - Contains Quick Start section with ≤5 commands
    - Contains Tech Stack table
    - Contains Evaluation Results table
    - Contains Project Structure tree
    - Contains API endpoint table
    - Renders correctly when viewed in a markdown preview
  </verify>
  <done>
    README.md is a comprehensive portfolio showcase with architecture diagram (Mermaid), CI badge, quick start instructions, tech stack table, evaluation metrics summary, project structure, and API documentation. A hiring manager can evaluate the project's scope and quality from the README alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build evaluation dashboard component and create demo/portfolio preparation artifacts</name>
  <files>
    src/frontend/src/components/dashboard/eval-dashboard.tsx
    src/frontend/src/App.tsx
    docs/demo-script.md
  </files>
  <action>
    Build the evaluation dashboard and prepare remaining portfolio artifacts (PORT-03, PORT-04, PORT-05).

    1. **Evaluation Dashboard** (`dashboard/eval-dashboard.tsx`):
       - A React component that displays evaluation metrics in a visually appealing layout
       - Data source: import a bundled JSON file. Create `src/frontend/public/eval-results.json` with sample/default metric values matching the evaluation output schema:
         ```json
         {
           "context_recall": 0.82,
           "context_precision": 0.78,
           "faithfulness": 0.91,
           "answer_relevancy": 0.85,
           "refusal_accuracy": 0.95,
           "citation_accuracy": 0.98,
           "hallucination_rate": 0.03,
           "benchmark": {
             "naive_recall": 0.45,
             "hybrid_recall": 0.82,
             "improvement": "82%"
           }
         }
         ```
       - Layout: Responsive grid of metric cards (3 cols lg, 2 md, 1 sm)
       - Each metric card (use shadcn Card):
         - Metric name (e.g., "Context Recall")
         - Current value displayed large (e.g., "82%")
         - Target threshold (e.g., "Target: > 75%")
         - Pass/fail indicator: green checkmark if meets target, red X if below
         - Visual bar showing value relative to target (simple div with width% and background color)
       - Benchmark comparison section at bottom:
         - Side-by-side card: "Naive Retrieval" vs "Hybrid Retrieval" showing context recall comparison
         - Improvement percentage highlighted
       - Header text: "Evaluation Dashboard — Automated Quality Metrics"
       - Subtitle: "Metrics are automatically computed by CI/CD pipeline against a golden dataset of 51 Q&A pairs"
       - Note at bottom: "Last updated: {date from JSON or 'Run scripts/evaluate.py to refresh'}"
       - Fetch the JSON from `/eval-results.json` using a simple fetch (or import as static asset)

    2. **Update App.tsx** — add a third tab "Dashboard" that renders EvalDashboard component. Tab order: Chat | Documents | Dashboard.

    3. **Demo Video Script** (`docs/demo-script.md`):
       Create a detailed script for a 2-minute demo video (PORT-04):
       ```markdown
       # RAGReady Demo Video Script (2 minutes)

       ## Setup
       - Screen recording tool: OBS Studio or built-in screen recorder
       - Browser open to http://localhost:5173
       - Backend running on port 8000
       - Have a sample .md or .txt document ready

       ## Timeline

       ### 0:00-0:15 — Introduction
       - Show README hero section in GitHub
       - "RAGReady: A production-grade RAG system with citation-enforced answers"

       ### 0:15-0:45 — Document Upload
       - Switch to frontend, Documents tab
       - Click Upload → select sample document
       - Show success toast with chunk count
       - "Documents are chunked and indexed in both dense and sparse indexes"

       ### 0:45-1:15 — Question & Answer
       - Switch to Chat tab
       - Type a specific question about the document
       - Show answer appearing with confidence badge
       - Click message → show citations panel
       - "Every answer is grounded in retrieved evidence with verifiable citations"

       ### 1:15-1:30 — Refusal Demo
       - Ask an off-topic question (e.g., "What is the weather today?")
       - Show refusal response with explanation
       - "The system refuses to answer rather than hallucinate"

       ### 1:30-1:50 — Evaluation & CI
       - Switch to Dashboard tab
       - Show metric cards with pass/fail indicators
       - Show benchmark comparison (naive vs hybrid)
       - "Automated evaluation runs on every PR with quality gates"

       ### 1:50-2:00 — Closing
       - Show architecture diagram from README
       - "Full source code, documentation, and CI pipeline available on GitHub"
       ```

    4. **CI Screenshot Instructions** — Add a note in `docs/demo-script.md` for PORT-05:
       ```markdown
       ## CI Quality Gate Screenshot (PORT-05)

       To capture the CI quality gate screenshot:
       1. Push a change to a branch and create a PR
       2. Wait for GitHub Actions to complete
       3. Screenshot the PR checks showing all jobs passing (or blocking on metric regression)
       4. Save as `docs/ci-quality-gate.png`
       5. Reference in README: `![CI Quality Gate](docs/ci-quality-gate.png)`

       For a regression demo: temporarily lower a metric threshold in a test,
       push to a PR branch, and screenshot the failing check.
       ```
  </action>
  <verify>
    - `pnpm build` in src/frontend/ passes
    - Dashboard tab renders in the app showing metric cards with values
    - Each metric card shows pass/fail indicator correctly
    - Benchmark comparison section shows naive vs hybrid
    - `docs/demo-script.md` exists with timestamped script
    - README.md references the dashboard
  </verify>
  <done>
    Evaluation dashboard component renders metric cards with pass/fail indicators and benchmark comparison (PORT-03). Demo video script with timestamps and talking points exists (PORT-04). CI screenshot instructions documented (PORT-05). README links to dashboard. All portfolio artifacts are either created or documented with clear instructions for the manual steps (video recording, CI screenshot).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete portfolio presentation</name>
  <files>README.md</files>
  <action>
    Human verification of all portfolio artifacts. What was built: Portfolio-ready README with architecture diagram + evaluation dashboard route in React app + demo video script. The complete RAGReady project should now be impressive to a hiring manager viewing the GitHub repo.

    Verification steps:
    1. Open README.md in a markdown previewer or GitHub — verify:
       - Architecture diagram renders correctly (Mermaid)
       - Quick start instructions are clear and complete
       - Tech stack table is accurate
       - Evaluation metrics table shows all 7 metrics with targets
       - Project structure tree is accurate
       - Overall impression: would this impress a hiring manager?

    2. Open http://localhost:5173 (frontend running):
       - Click "Dashboard" tab
       - Verify metric cards render with values and pass/fail indicators
       - Verify benchmark comparison section (naive vs hybrid)
       - Verify the layout looks professional

    3. Review `docs/demo-script.md`:
       - Script covers all required segments (upload, query, refusal, evaluation, CI)
       - Timestamps add up to ~2 minutes
       - Instructions are clear enough to follow

    4. Overall checklist:
       - README alone tells the full project story
       - Frontend is functional and visually polished
       - Dashboard shows quality metrics
       - Demo script is actionable
       - CI screenshot instructions are documented
  </action>
  <verify>User confirms portfolio quality by typing "approved"</verify>
  <done>All portfolio artifacts verified by user: README impresses hiring managers, dashboard shows metrics, demo script is actionable, CI screenshot process documented.</done>
</task>

</tasks>

<verification>
- README.md contains Mermaid architecture diagram (PORT-01)
- README.md has CI badge, quick start, architecture overview (PORT-02)
- Evaluation dashboard renders in React app with metric cards (PORT-03)
- Demo video script exists with timestamps (PORT-04)
- CI screenshot instructions documented (PORT-05)
- `pnpm build` passes with no errors
- All three tabs (Chat, Documents, Dashboard) render correctly
</verification>

<success_criteria>
1. README includes Mermaid architecture diagram showing all 5 layers (PORT-01)
2. README includes CI badge, quick start (≤5 commands), and comprehensive tech overview (PORT-02)
3. Evaluation dashboard route shows metric cards with pass/fail indicators and benchmark comparison (PORT-03)
4. Demo video script exists with timestamps for a 2-minute walkthrough (PORT-04)
5. CI quality gate screenshot instructions are documented (PORT-05)
6. A hiring manager viewing the GitHub repo can evaluate the project's scope and quality from the README alone
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-portfolio/04-02-SUMMARY.md`
</output>
