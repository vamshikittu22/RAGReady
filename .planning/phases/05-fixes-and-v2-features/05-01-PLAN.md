---
phase: 05-fixes-and-v2-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false

must_haves:
  truths:
    - "A comprehensive diagnosis of any failing tests, backend timeouts, or frontend bugs is performed"
    - "All identified critical bugs from Phase 1-4 are fixed and tested"
    - "Streaming responses (Server-Sent Events) are added to the backend API and integrated into the frontend UI"
    - "The roadmap and project state are updated to reflect the new Phase 5 progress"
  artifacts:
    - path: "src/ragready/api/routes.py"
      provides: "Streaming capability via Server-Sent Events for query responses"
  key_links: []
---

<objective>
Diagnose and fix any broken features or failing tests from previous phases, and begin implementing V2 features (like streaming responses) to elevate the project beyond its V1 capabilities. Keep this phase ready for the user's approval before full execution.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Diagnose and Fix "Not Working" Components</name>
  <files>
    * (Various depending on findings)
  </files>
  <action>
    1. Discuss with the user to identify specifically which components are "not working" (e.g., UI rendering issues, backend API errors, slow test suites, LLM connection failures).
    2. Run the test suite `pytest` and trace any hanging or failing tests. Provide fixes for them.
    3. Verify that the RAG pipeline correctly handles the existing document dataset without index corruption.
    4. Confirm that the UI cleanly handles errors, backend timeouts, and loading states.
  </action>
  <verify>
    - All tests pass locally without hanging.
    - The UI can successfully query the backend.
  </verify>
  <done>
    Bugs and regressions from previous phases are fully diagnosed and patched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Streaming Responses (V2 Feature)</name>
  <files>
    src/ragready/api/routes.py
    src/frontend/src/lib/api.ts
    src/frontend/src/hooks/use-query-rag.ts
    src/frontend/src/components/chat/chat-interface.tsx
  </files>
  <action>
    1. Modify the backend FastAPI `/query` endpoint (or add `/query/stream`) to yield Server-Sent Events (SSE) tokens from the LLM.
    2. Update the frontend `api.ts` to consume the SSE stream (e.g., using `fetch` with a `ReadableStream` or an `EventSource`).
    3. Update the `use-query-rag` hook and the Chat interface to append tokens incrementally to the active message box, offering a real-time "typing" effect.
  </action>
  <verify>
    - Calling the streaming endpoint returns tokens in chunks rather than a single blocked JSON response.
    - Frontend correctly accumulates the chunks into a readable message.
  </verify>
  <done>
    Streaming is fully functional from backend generation to frontend display.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Planning and Documentation</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    README.md
  </files>
  <action>
    1. Append Phase 5 to `.planning/ROADMAP.md`.
    2. Update `.planning/STATE.md` to reflect that we are in Phase 5: Fixes and Enhancements.
    3. Add notes to `README.md` about the new Streaming support once deployed.
  </action>
  <verify>
    - Planning documents accurately reflect Phase 5 as the current active step.
  </verify>
  <done>
    Project state is up to date and reflects the new V2 trajectory.
  </done>
</task>

</tasks>

<verification>
- User has explicitly approved which regressions to fix.
- Test suites run green.
- Streaming component works smoothly without breaking structured JSON citations.
</verification>

<success_criteria>
1. All "not working" items noted by the user are resolved.
2. The user has approved the plan before automatic tasks run.
3. The codebase demonstrates advanced LLM engineering through SSE streaming.
</success_criteria>

<output>
After completion, create `.planning/phases/05-fixes-and-v2-features/05-01-SUMMARY.md`
</output>
